import * as React from 'react';
import { Layout } from '../components/Layout/Layout';

const ContactPage = () => {
  return (
    <Layout>
      <div>Contact</div>
    </Layout>
  );
};

export default ContactPage;
